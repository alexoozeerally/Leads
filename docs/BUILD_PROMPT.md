# Build Prompt — AI Web-Design Lead Finder

This document is the authoritative build plan. It is preserved here verbatim in
intent: build a modular Python platform that discovers UK businesses, audits
their online presence, scores them as web-design prospects, and drafts outreach
for human review — built in phases, runnable and tested at every checkpoint.

See the repository `README.md` for how to run it and `PROGRESS.md` for what is
live at each phase. The phase plan (0–7), core contracts, AI-agent rules, and
UK compliance obligations are implemented as described there.
